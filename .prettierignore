# Generated
.next/
out/
coverage/
next-env.d.ts
*.tsbuildinfo

# Lockfile — pnpm owns the formatting
pnpm-lock.yaml

# 18 MB of ASCII frames. Formatting it would rewrite every line and, worse,
# trailing whitespace is load-bearing here: it sets the <pre> box width.
public/ascii/frames.txt

# PocketBase runtime data and binary
pb_data/
tools/

# Carried over verbatim from V1. Reformatting rewrote ~600 lines across these
# and destroys the ability to diff them against the originals. They are
# reference material, not source.
AGENTS.md
docs/PLATFORM.md
docs/tracker-handbook.md
