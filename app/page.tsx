import { Hero } from '@/components/features/landing/hero'

/**
 * Landing page. Only the hero is carried over from V1 — the remaining sections
 * are being redesigned and will be rebuilt against the new UI plan.
 */
export default function Home() {
  return (
    <main>
      <Hero />
    </main>
  )
}
