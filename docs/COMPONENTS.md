# Component inventory

**Read this before writing a new component.** Most of what a page needs already exists,
themed and accessibility-checked. Several of these are currently unused — they were built
for pages that are being redesigned, not abandoned.

Surfaces referenced below:

- **paper** — the light page background (`--background`)
- **navy** — `eng-navy`, used for the dashboard and mobile menu
- **maroon** — `argc-maroon`, the hero and registration background

---

## In use

| Component            | File                                                | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| -------------------- | --------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Navbar`             | `components/shared/navbar.tsx`                      | Sticky, scroll-aware, mobile hamburger with scroll lock and a profile dropdown. Links come from `content/site.json`. Reads auth from `useAuth()` — do not add another session fetch. No Register CTA: the public site is a login-free showcase. `maroon` variant on `/register`; `default` goes opaque over hero-less paper pages (`/events`, `/blog`); `dashboard` variant on `/dashboard/*` drops NAV_LINKS and its own mobile takeover entirely — `DashboardSidebar` owns dashboard nav instead.                                                                                                                                                                                                         |
| `Hero`               | `components/features/landing/hero.tsx`              | Full-viewport maroon hero. Copy from `content/landing.json`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `AsciiCanvas`        | `components/features/landing/ascii-canvas.tsx`      | 24fps ASCII animation. Fetches `public/ascii/frames.txt`; **never import the frames as a module** — that cost 18.4 MB of JavaScript in V1. Respects reduced motion, pauses on tab-blur and off-screen.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `OverviewContent`    | `components/features/member/overview-content.tsx`   | Personal summary (MEMBER-03): node name line, raw XP readout + `XpBar`, evaluation status, upcoming events. `nodeName: string \| null`, `xp: number \| null`, `evals: { stage, status }[] \| null`, `events: OverviewEvent[]`. Each section tolerates its own "nothing yet" state. Copy from `content/dashboard-overview.json`. Page-local in the branch it landed in; relocated here per DASHBOARD_CONTRACT §3 ("never page-local components").                                                                                                                                                                                                                                                            |
| `DashboardShell`     | `components/shared/dashboard/dashboard-shell.tsx`   | Navy layout wrapper rendered by `app/dashboard/layout.tsx` for every `/dashboard` route. Sidebar rail + scrollable content column; `pt-nav` clears the fixed public Navbar.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `DashboardSidebar`   | `components/shared/dashboard/dashboard-sidebar.tsx` | Rendered unconditionally inside `DashboardShell`. Role-filtered nav, mobile takeover menu; renders `Avatar` + `RoleBadge` for the signed-in user.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `DashboardHeader`    | `components/shared/dashboard/dashboard-header.tsx`  | `title`, `crumbs?`, `actions?`. Used directly by `app/dashboard/page.tsx`, `app/dashboard/overview/page.tsx` and `app/dashboard/node/events/propose/page.tsx`. The overview and propose pages carry real content; `dashboard/page.tsx` is still a redirect shell.                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `Avatar`             | `components/shared/avatar.tsx`                      | `src`, `name`, `size?`, `surface?`. Not just dev-preview scaffolding — `DashboardSidebar` renders it for every signed-in dashboard visitor. Distinct from the shadcn `avatar` primitive in `components/ui/`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `RoleBadge`          | `components/shared/dashboard/role-badge.tsx`        | `role: Role`. Rendered alongside `Avatar` in `DashboardSidebar`'s identity block — reachable now, not only via the dev preview.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `ErrorFallback`      | `components/shared/error-fallback.tsx`              | Body for every `error.tsx`, `surface?` (`dark` \| `light`). Used by the overview and node-propose error boundaries — shows the raw message only in development, surfaces `digest` in production.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `LoadingRow`         | `components/shared/dashboard/loading-row.tsx`       | `surface?`, `className?`. Bordered-row skeleton (`h-[46px]`/`border-b`); rendered by the overview loading boundary.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `ProposeEventClient` | `components/features/node/propose-event-client.tsx` | Page bridge for `/dashboard/node/events/propose` (MEMBER-14). Owns the same-origin POST to `/api/dashboard/node/events` and maps the route's envelope onto `EventProposeForm`'s discriminated result (past/invalid/default).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `EventProposeForm`   | `components/features/node/event-propose-form.tsx`   | `onPropose(input)` → `{ ok: true } \| { ok: false; reason: 'past' \| 'invalid' \| 'unknown' }`. Node leader filing surface (MEMBER-14) that POSTs a proposal to the node's pipeline. Same validation discipline as `EvaluationScheduleForm`: inline per-field errors, JS validation with no native `required`, a client-side future check on the `datetime-local` `starts_at`, and server rejection — including the route's literal `starts_at must be in the future` — mapped to user-safe copy per reason. Copy from `content/node-events.json`. Wired to the page by `propose-event-client.tsx`, which owns the same-origin POST (the HttpOnly auth cookie rides along) and the envelope→result mapping. |
| `PostRow`            | `components/features/blog/post-row.tsx`             | One post in the `/blog` index as a horizontal row: 16:9 thumbnail (grayscale → colour on hover, `BannerPlaceholder` fallback), title column, mono metadata, READ → affordance. Whole row is a single `<Link>` to `/blog/{slug}`. `post: PostView` from `lib/blog.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `PostBody`           | `components/features/blog/post-body.tsx`            | Renders the sanitized rich-text body in `.article-body` typography. Never sanitizes — content arrives sanitized from `lib/blog.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `AuthorLine`         | `components/features/blog/author-line.tsx`          | Post byline: 24px avatar (monogram fallback when `avatar_url` is empty) + mono name under the upcased label. `author: PostAuthor \| null`, returns null when absent.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `Tag`                | `components/features/blog/tag.tsx`                  | Flat bracket label `[LIKE THIS]` — mono, all-caps. Not a pill.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `EmptyState`         | `components/shared/empty-state.tsx`                 | Used by `/blog` (no posts yet) and the other "no records" surfaces. `title`, `description?`, `icon?`, `action?`, `surface?` (`dark` \| `light`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `BannerPlaceholder`  | `components/shared/banner-placeholder.tsx`          | Stands in for a missing `posts.banner`/event poster — deterministic gradient derived from a `seed` string so a card never changes between renders.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

---

## Available, currently unused

### Shared

| Component                            | File                                                   | Props / behaviour                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------------------------------------ | ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `Field`, `FieldArea`, `FieldWrapper` | `components/shared/field.tsx`                          | Labelled input and textarea. `surface: 'dark' \| 'light'`. Ids come from `useId()`; error text is wired to `aria-describedby` and borders meet the 3:1 non-text contrast rule.                                                                                                                                                                                                                                                                                                                                                 |
| `Spinner`                            | `components/shared/spinner.tsx`                        | `size: 'sm' \| 'md' \| 'lg'`, `label` for screen readers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `Breadcrumb`                         | `components/shared/breadcrumb.tsx`                     | `items: { label, href? }[]`. The final crumb renders as text with `aria-current="page"`.                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Skeletons                            | `components/shared/loading-skeleton.tsx`               | `CardSkeleton`, `CardGridSkeleton`, `RowSkeleton`, `RowListSkeleton`, `DetailSkeleton` (in use by the blog detail loading boundary), plus the blog list shapes `PostRowSkeleton`/`PostListSkeleton`/`BlogIndexSkeleton`. Shaped to match real layouts so loading does not shift content.                                                                                                                                                                                                                                       |
| `TierBadge`                          | `components/shared/dashboard/tier-badge.tsx`           | `tier: Tier`. 4-segment tick bar (gray → Steel Blue → Signal Amber → Alert Coral), deliberately a different construction from `RoleBadge` so rank and tier read as different facts. Real path — `TASKS_BACKLOG.md`'s UI-07 and MEMBER-07 assumed `components/shared/xp/tier-badge.tsx`; that path was never used.                                                                                                                                                                                                              |
| `EvaluationStageRow`                 | `components/shared/dashboard/evaluation-stage-row.tsx` | `stage`, `status`, `evaluatorName`, `scheduledAt`, `score`, `surface?`. Read-only Bordered Rows template (fixed 46px height, `border-b` only, per-field `aria-label`). Promoted to `shared/` under `DASHBOARD_CONTRACT.md` §3's "two or more features need it" rule — the member's own evaluation history, the node leader's schedule view, and the super peer's pipeline view all consume it. Supersedes `TASKS_BACKLOG.md` MEMBER-13's assumed `components/features/evaluations/evaluation-stage-row.tsx`.                   |
| `NodeMemberRow`                      | `components/shared/dashboard/node-member-row.tsx`      | `name`, `avatarUrl`, `tier`, `xp`, `evaluationStages`, `href?`, `surface?`. Same Bordered Rows template; optional `href` turns the row into a link with hover/focus states, absent it stays fully static. Promoted to `shared/` under `DASHBOARD_CONTRACT.md` §3's "two or more features need it" rule — the member's own node roster, the node leader's member table, and the super peer's member directory all consume it. Supersedes `TASKS_BACKLOG.md` MEMBER-10's assumed `components/features/node/node-member-row.tsx`. |
| `RoleGate`                           | `components/shared/dashboard/role-gate.tsx`            | `minRole`, `children`, `fallback?`. Not rendered anywhere in the real component tree — `DashboardShell`/`Sidebar`/`Header` never use it. Only exercised today in the dev preview route's demo blocks.                                                                                                                                                                                                                                                                                                                          |
| `StatusChip`                         | `components/shared/dashboard/status-chip.tsx`          | `domain`, `status`, `surface?`, `className?`, `ariaLabel?` (overrides the default `Status: …` accessible name). Consumed by `EvaluationStageRow` and `CycleSelector`, neither wired into a real page yet — reachable only via the dev preview.                                                                                                                                                                                                                                                                                 |
| `XpBar`                              | `components/shared/dashboard/xp-bar.tsx`               | `xp: number`, `surface?`, `className?`. Progress toward the next tier threshold via `tierProgress()` (`lib/constants.ts`); Maroon fill regardless of surface, fully-filled + "TOP TIER — {TIER}" label at the max tier. Real path — `TASKS_BACKLOG.md`'s UI-06 assumed `components/shared/xp/xp-bar.tsx`; that path was never used, same as `TierBadge`'s UI-07.                                                                                                                                                               |
| `StatCard`                           | `components/shared/dashboard/stat-card.tsx`            | `label`, `value: string \| number`, `delta?: { direction, tone, text }`, `surface?`, `className?`. `direction` (up/down/flat) drives the arrow, `tone` (positive/negative/neutral) drives the color — kept independent so a "lower is better" metric moving the right way doesn't render coral. Also exports `deltaFor(polarity, from, to, text)` to derive both from one declared metric polarity.                                                                                                                            |
| `XpLedgerTable`                      | `components/shared/dashboard/xp-ledger-table.tsx`      | `entries: XpLedgerEntry[]`, `rangeLabel`, `prevHref`, `nextHref`, `surface?`, `className?`. Real `<table>`/`<th scope="col">` markup, not the shadcn `table` primitive in `components/ui/` — that primitive still has no real consumer. Amount is signed mono (`+25`/`-10`/`0`); Source resolves to `awarded_by`'s display name (sans) or the literal "System" token (mono) for a null award. Real path — `TASKS_BACKLOG.md`'s UI-11 assumed `components/shared/xp/xp-ledger-table.tsx`.                                       |
| `CycleSelector`                      | `components/shared/dashboard/cycle-selector.tsx`       | `cycles: AdvancementCycleRecord[]`, `currentCycleId`, `hrefFor: (cycle) => string`, `surface?`, `className?`. Resolves UI-12's open "dropdown or segmented control" choice to dropdown-only — cycles accumulate for the platform's life with no archival step, so a fixed-width tab row has no scaling path. Rows are real `<Link>`/`<button disabled>` elements (arrow-key/Home/End roving focus layered on top), not `role="option"`, since selecting one always navigates rather than reporting a value into a form.        |

### Features

| Component                | File                                                    | Props / behaviour                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------------------ | ------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `VoteCastForm`           | `components/features/vote/vote-cast-form.tsx`           | Member-facing vote casting (MEMBER-12). Props: `eligible`, `myVotes`, `onCast`. Confirmation step before submit; reason bounded client-side to the same 10–500 the POST route enforces; spent polarity disabled; maps a `conflict` cast result to user-safe copy. Copy from `content/vote.json`.                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `EvaluationScheduleForm` | `components/features/node/evaluation-schedule-form.tsx` | `evaluatee: { id, display_name }`, `evaluators: { id, display_name }[]`, `onSchedule(input)` → `{ ok: true } \| { ok: false; reason: 'conflict' \| 'invalid' \| 'unknown' }`. Node leader scheduling surface (MEMBER-09) that drives the `pending → scheduled` PATCH. Local `datetime-local` picker converted to a UTC ISO `scheduled_at` (the UTC calendar day is what counts for on-time XP); optional evaluator `<select>` that always excludes the evaluatee; empty-date blocked with inline alert, not native `required`; `onSchedule`'s `conflict`/`invalid` and any thrown rejection all map to user-safe copy and a retry. Renders via `Field`/`FieldWrapper`/`Button`. Copy from `content/evaluations.json`. |
| `EvaluationPipelineCard` | `components/features/node/evaluation-pipeline-card.tsx` | `member: { id, display_name, avatar_url }`, `evaluations: PipelineEvaluation[]`. One member's evaluation pipeline as a card unit: `Avatar` + display name header, then each present stage as an `EvaluationStageRow` in canonical order (Stage 1 → Stage 2 → Stage 3 Node Leader) regardless of input order; never invents a stage the platform didn't create; `EmptyState` copy when none. Presentational — the page owns fetching `GET /node/evaluations` and filters this card's slice. Copy from `content/evaluations.json`.                                                                                                                                                                                      |

`VoteCastForm`, `EvaluationScheduleForm` and `EvaluationPipelineCard` are built, tested and
catalogued but are **not yet on a real screen**: the vote form awaits `/dashboard/vote`
(MEMBER-13) and the evaluation pair awaits `/dashboard/node/evaluations` (MEMBER-11).

### shadcn primitives

`components/ui/` — `button`, `card`, `input`, `textarea`, `dialog`, `skeleton`,
`dropdown-menu`, `table`, `badge`, `avatar`, `separator`.

Rethemed to the ARGC palette: square corners (`--radius: 0`), maroon primary, cool
neutrals. They are generated files — prefer composing them over editing them, and re-add
others with `npx shadcn@latest add <name>` rather than hand-writing a primitive.

---

## Library modules

| Module                     | Purpose                                                                                                                                                                                                                                                                        |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `lib/auth.ts`              | `authenticate()`, `requireRole()`, `requireSuperPeer()`, `requireSuperAdminPeer()`, and `AuthError` carrying a 401/403 status. Use `authErrorResponse()` in route handlers so internal errors do not leak.                                                                     |
| `lib/auth-context.tsx`     | `AuthProvider` + `useAuth()`. One `/api/auth/me` call per page load — components must not fetch it themselves.                                                                                                                                                                 |
| `lib/sanitize.ts`          | `sanitizeHtml()` and `stripHtml()`. Call at fetch time, never during render. Covered by `lib/sanitize.test.ts`.                                                                                                                                                                |
| `lib/constants.ts`         | Role hierarchy, `roleAtLeast()`, route prefixes, `isSafeRedirect()`, XP categories, tiers, cookie names.                                                                                                                                                                       |
| `lib/content.ts`           | Typed accessors for `content/*.json`. Import from here, never the JSON directly.                                                                                                                                                                                               |
| `lib/cookies.ts`           | Shared auth cookie options so they cannot drift between routes.                                                                                                                                                                                                                |
| `lib/pocketbase-server.ts` | `getAdminClient()` (cached + HMR-safe) and `getPocketBaseClient()`.                                                                                                                                                                                                            |
| `lib/dashboard-fetch.ts`   | `fetchDashboardApi(path)` — server-component fetch of this app's own `/api/dashboard/*` routes with the caller's session cookie forwarded. Returns the raw `Response`; callers decide their own tolerance. The standard data path for dashboard pages (DASHBOARD_CONTRACT §5). |
| `lib/xp.ts`                | `awardXp()` — the single ledger-write path for XP (idempotent, keeps `user_stats` in sync). Every XP award goes through it. See ARCHITECTURE.md. Covered by `lib/xp.test.ts`.                                                                                                  |
| `lib/api/42-api.ts`        | 42 Intra OAuth URL, code exchange, profile fetch.                                                                                                                                                                                                                              |

---

## Adding a component

1. Check this file first.
2. One feature → `components/features/<domain>/`. Used by two or more → `components/shared/` (ownership: see `docs/DASHBOARD_CONTRACT.md` §3).
3. Style with tokens (`bg-paper`, `text-ink-muted`, `border-input`), never raw hex.
4. Server component by default; add `'use client'` only when you need state or an effect.
5. Colocate a `*.test.tsx` and render it through `test/render.tsx`.
6. Add a row here.
